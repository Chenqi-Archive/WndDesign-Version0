放开部分权限以提高效率（TileWnd不再进行检查、board不需要请求，可以直接画）

定义特殊区域（自动分配、居中、特殊位置停靠等等）

鼠标跟踪（设置活动窗口）

计时器类与动画分离

窗口动画效果（滚动条自动隐藏、窗口出现时的动画）

图像性能优化

board简化+优化（SafeSet和DirectSet）（只保留可行区域）

board.Clear()

窗口框架（边框 > 标题栏 > 滚动条等）分离

边框显示区域和鼠标响应区域分离（直接覆盖底层窗口）

窗口（bitmap）的缩放和resize

透明效果（AlphaBlend）

支持临时窗口（对于TileWnd）（需要确保底层不会重画）保存底层图像，销毁时再还原

滚动条框架参数：最大范围、当前可见范围

滚动窗口应该为所有子窗口独立分配bitmap，（背景保持不动），需要空间索引快速定位。

TileWnd/SplitWnd不支持滚动

显示线程和消息线程分开